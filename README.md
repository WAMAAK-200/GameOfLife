# GameOfLife
An enhanced version of Conway's Game of Life with extra features extending past just random generation but the ability to save, load and create designs


## How to run it

Run the program as usual through Python, due to its sheer size try to run it at fullscreen.

NOTE: You may need to install the keyboard module for this project, if you have not already done so make sure to use `pip install keyboard`
